int valor = 4;
valor += 2;
valor -= 1;
valor *= 3;
valor /= 2;
if (valor == 5 && valor != 0 || !false) {
    valor++;
    valor--;
}
// comentario de línea
/* comentario de bloque */
char salto = '\n';
